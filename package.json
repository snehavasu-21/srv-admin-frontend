{
  "name": "srv-admin-frontend-runner",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "npm --prefix srv-admin-frontend run build",
    "start": "npm --prefix srv-admin-frontend run start",
    "lint": "npm --prefix srv-admin-frontend run lint"
  }
}
